{
  // Deploys this repo as a Cloudflare Pages/Workers static site named "recess"
  // (no build step, no Worker code). Create the project on first deploy.
  "name": "recess",
  "compatibility_date": "2026-07-08",
  "assets": {
    "directory": "."
  }
}
